import { AppRoutes, ScreenProps } from "@navigation/appRoutes";
import React from "react";
import { View } from "react-native";

const UserInventory: React.FC<ScreenProps<AppRoutes.Shop>> = () => {
  return <View></View>;
};

export default UserInventory;

import styled from "@emotion/native";

const Container = styled.View`
  flex: 1;
  gap: 24px;
  position: absolute;
  top: 0;
  right: 0;
  flex-direction: row;
  z-index: 1;
  align-items: center;
`;

export default { Container };

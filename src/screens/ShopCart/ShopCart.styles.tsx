import styled from "@emotion/native";

const Container = styled.View`
  flex: 1;
  padding: 24px;
  gap: 24px;
`;

const Header = styled.View`
  gap: 12px;
`;

const VerticalScroll = styled.ScrollView`
  flex: 1;
`;

export default { Container, Header, VerticalScroll };
